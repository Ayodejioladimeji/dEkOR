import React from "react";

interface Props {}

const Search = (props: Props) => {
  return (
    <div className="search-input">
      <input type="text" />
    </div>
  );
};

export default Search;
